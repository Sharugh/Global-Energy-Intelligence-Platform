# Global DC Intelligence — pinned requirements
# Generated May 2026. Pin to these versions for reproducible Streamlit Cloud deploys.
# Bump versions deliberately; do not auto-upgrade without testing.

streamlit>=1.35.0,<2.0.0

# Data
pandas>=2.2.0,<3.0.0
plotly>=5.22.0,<6.0.0
openpyxl>=3.1.2,<4.0.0

# Web scraping & HTTP
requests>=2.32.0,<3.0.0
cloudscraper>=1.2.71           # optional — falls back to requests if absent
beautifulsoup4>=4.12.3,<5.0.0
feedparser>=6.0.11,<7.0.0
lxml>=5.2.0                    # faster BS4 parser (optional but recommended)

# Export: Word (.docx)
python-docx>=1.1.2,<2.0.0

# Export: PDF
reportlab>=4.2.0,<5.0.0

# Python standard library (no install needed — listed for documentation only):
# re, io, time, math, textwrap, collections, datetime, concurrent.futures, difflib, json, os
